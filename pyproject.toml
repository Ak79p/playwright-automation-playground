[project]
name = "automation-playground"
version = "0.1.0"
description = "AI-assisted Playwright test automation (Python + pytest) for uitestingplayground.com"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-playwright>=0.9.0",
]
